##This is a simple sender + receiver application##

To build from top directory:

`cmake -S . -B build`
`cmake --build build`

*NOTE: CMakeLists.txt includes building zmq library from source so if you wipe the build clean, it will take much longer.*

